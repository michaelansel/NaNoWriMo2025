# Environment variables for continuity-webhook service
#
# Copy this file to /home/ubuntu/.config/continuity-webhook/env
# and fill in the values

# ========================================
# Authentication Configuration
# ========================================

# Option 1: GitHub App (Recommended)
# Comments will appear as "app-name[bot]" instead of your user account
# Leave these blank to use PAT fallback
#
# To create a GitHub App:
# 1. Go to Settings → Developer settings → GitHub Apps → New GitHub App
# 2. Set required permissions: Actions (read), Contents (read/write), Issues (write), PRs (write)
# 3. Generate and download private key (.pem file)
# 4. Install app on your repository
# 5. Get App ID from app settings page
# 6. Get Installation ID from installation URL or API
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY_PATH=
GITHUB_APP_INSTALLATION_ID=

# Option 2: Personal Access Token (Fallback)
# Used if GitHub App credentials are not provided
# Generate at: https://github.com/settings/tokens
# Required permissions: repo (full)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ========================================
# Webhook Configuration
# ========================================

# Webhook secret for signature verification
# Generate with: openssl rand -hex 32
# This must match the secret configured in GitHub webhook settings
WEBHOOK_SECRET=your_webhook_secret_here

# ========================================
# Repository Configuration
# ========================================

# Repository information
REPO_OWNER=michaelansel
REPO_NAME=NaNoWriMo2025

# Port for webhook service (default: 5000)
WEBHOOK_PORT=5000
